{
  "id": "json-omit-none",
  "task": "Make omit_none() recursively omit dictionary keys whose value is None, recurse through nested dictionaries and lists, and preserve list positions.",
  "setup_files": {
    "payloads.py": "def omit_none(value):\n    return {key: item for key, item in value.items() if item is not None}\n",
    "test_payloads.py": "from payloads import omit_none\n\ndef test_top_level_none():\n    assert omit_none({'a': 1, 'b': None}) == {'a': 1}\n"
  },
  "oracle_command": ["python", "-m", "pytest", "-q"],
  "gold_files": {
    "payloads.py": "def omit_none(value):\n    if isinstance(value, dict):\n        return {key: omit_none(item) for key, item in value.items() if item is not None}\n    if isinstance(value, list):\n        return [omit_none(item) for item in value]\n    return value\n"
  }
}
